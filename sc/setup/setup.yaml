sc_yaml: "sc.yaml"
default_plant_uml: "uml.tpl"      
after_structure_creation:
- "go mod init {{ Cfg.Module }}"
- "go get github.com/onsi/gomega/..."
- "go get github.com/onsi/ginkgo/v2"
after_all:
- "make sc"
files:
- src: "main.tpl"
  target: "main.go"
- src: "Makefile.tpl"
  target: "Makefile"